/// @description Insert description here
// You can write your code in this editor
if (current_health <= 0) {
    sprite_index = spr_candy_queen_dead; // Change to death sprite


    // Calculate the number of steps to finish the 6-frame animation
	for (var i = 0; i < dropChance; i++) {
    instance_create_layer(x, y, "Instances", obj_recipe);
}
   instance_destroy()
   instance_create_layer(1984,1424,"Instances",obj_end_boss_door)
}
